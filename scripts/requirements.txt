# Requirements for LHA Memoirs processing scripts
#
# IMPORTANT: Install PyTorch with CUDA first, then the rest:
#   uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   uv pip install -r requirements.txt
#
# Or use the setup script: uv run setup_env.py

# PyTorch (install with CUDA support using command above)
# If installed via requirements.txt, this will be CPU-only:
torch>=2.0.0

# WhisperX for transcription with word-level alignment (includes faster-whisper)
whisperx>=3.7.0

# Ollama for LLM-based content analysis
ollama>=0.3.0

# Progress bars and CLI utilities
tqdm>=4.65.0
