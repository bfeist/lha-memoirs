import styles from "./readTheDocs.module.css";

const ReadTheDocs = () => {
  return (
    <p className={styles.readTheDocs}>
      Click on the Vite and React logos to learn more
    </p>
  );
};

export default ReadTheDocs;
